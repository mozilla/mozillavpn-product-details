{
  "name": "mozillavpn-product-details",
  "version": "1.0.0",
  "dependencies": {
    "ajv-cli": "^5.0.0"
  },
  "scripts": {
    "test": "ajv validate --spec=jtd -s schema.jtd.json -d 'docs/*.json' && ajv validate --spec=jtd -s schema_release.jtd.json -d docs/mozillavpn.json"
  }
}
