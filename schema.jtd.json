{
  "definitions": {
    "release": {
      "properties": {
        "date": {
          "type": "string"
        },
        "platforms": {
          "elements": {
            "enum": ["ANDROID", "IOS", "MAC", "WINDOWS", "LINUX"]
          }
        },
        "build": {"type": "uint8", "nullable": true}
      }
    }
  },
  "properties": {
    "latest": {
      "properties": {
        "ANDROID": {"type": "string"},
        "IOS": {"type": "string"},
        "LINUX": {"type": "string"},
        "MAC": {"type": "string"},
        "WINDOWS": {"type": "string"}
      }
    },
    "releases": {
      "values": {"ref": "release"}
    }
  }
}
